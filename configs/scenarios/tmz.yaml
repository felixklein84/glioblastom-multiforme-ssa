extends: ../default.yaml

# Stupp TMZ schedule (LIT); kill rates are PLACEHOLDERS / ASM.
therapy:
  type: tmz
  tmz:
    # Schedule — LIT (Stupp 2005)
    concomitant_start: 0.0
    concomitant_end: 42.0
    adjuvant_start: 49.0
    adjuvant_cycles: 6
    cycle_length_days: 28.0
    treatment_days: 5.0
    # Per-day kill rate during active TMZ — [ASSUMPTION, NOT CLINICALLY CALIBRATED]
    delta_S: 0.03    # GSC pre-existing resistance (qualitative; Lan 2017 motivation)
    delta_P: 0.20    # cycling progenitor — alkylation-sensitive
    delta_D: 0.05
    delta_Q: 0.01    # non-replicating — alkylation not converted to lethal damage
    # Per-day P→Q therapy-induced dormancy — [ASSUMPTION]
    sigma_T: 0.10

run:
  t_end: 365.0
